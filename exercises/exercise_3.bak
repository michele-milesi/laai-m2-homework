#lang gamble
(require gamble/viz)

; exercise 6
(define (geometric p)
  (if (flip p)
      0
      (+ 1 (geometric p))))

(define model
  (rejection-sampler
   (define A (geometric 0.5))
   A))

(hist (repeat model 100))